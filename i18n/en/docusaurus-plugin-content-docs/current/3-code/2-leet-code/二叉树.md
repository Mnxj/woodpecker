### Invert a binary tree

```js
function invertTree(root) {
    // Return early if the current node is null
    if (!root) {
        return null;
    }

    // Swap the left and right children
    [root.left, root.right] = [root.right, root.left];

    // Recursively invert the subtrees
    invertTree(root.left);
    invertTree(root.right);

    // Return the inverted tree
    return root;
}

// Example: build a simple binary tree
//     4
//    / \
//   2   7
//  / \ / \
// 1  3 6  9
const root = {
    val: 4,
    left: {
        val: 2,
        left: { val: 1, left: null, right: null },
        right: { val: 3, left: null, right: null }
    },
    right: {
        val: 7,
        left: { val: 6, left: null, right: null },
        right: { val: 9, left: null, right: null }
    }
};

// Invert
invertTree(root);

// Print the inverted tree
function printTree(node) {
    if (node === null) {
        return;
    }
    console.log(node.val);
    printTree(node.left);
    printTree(node.right);
}

printTree(root); // 4 7 9 6 2 3 1
```

### Zigzag level order traversal of the following binary tree

​    2
4    5
7  9  11 13

Expected output: 2 4 5 13 11 9 7

```js
class TreeNode {
    constructor(value) {
        this.value = value;
        this.left = null;
        this.right = null;
    }
}

function zigzagLevelOrder(root) {
    if (!root) return [];

    let result = [];
    let queue = [root];
    let leftToRight = true; // controls traversal direction

    while (queue.length > 0) {
        let levelSize = queue.length;
        let level = [];

        for (let i = 0; i < levelSize; i++) {
            let currentNode = queue.shift();

            // Add to the level based on the current direction
            if (leftToRight) {
                level.push(currentNode.value);
            } else {
                level.unshift(currentNode.value);
            }

            // Enqueue children
            item.left && queue.push(item.left);
            item.right && queue.push(item.right);
        }

        // Flip the direction
        leftToRight = !leftToRight;
        result.push(...level);
    }

    // Flatten all levels into one array
    return result.flat();
}

// Build the example tree
let root = new TreeNode(2);
let root = new TreeNode(2);
root.left = new TreeNode(4);
root.right = new TreeNode(5);
root.left.left = new TreeNode(7);
root.left.right = new TreeNode(9);
root.right.left = new TreeNode(11);
root.right.right = new TreeNode(13);

// Run zigzag traversal
console.log(zigzagLevelOrder(root)); // [2, 4, 5, 13, 11, 9, 7]
```


### 104. Maximum Depth of Binary Tree

Example 1:
Input: root = [3,9,20,null,null,15,7]
Output: 3

Example 2:
Input: root = [1,null,2]
Output: 2


**Solution 1**

DFS from the left.
Recursion base case:
1. null → return
1 (current depth) + max of dfs(left), dfs(right)
```js
var maxDepth = function(root) {
    if(root) return 0;
    return 1 + Math.max(maxDepth(root.left), maxDepth(root.right))

};
```


### 103. Binary Tree Zigzag Level Order Traversal

Example 1:
Input: root = [3,9,20,null,null,15,7]
Output: [[3],[20,9],[15,7]]

Example 2:
Input: root = [1]
Output: [[1]]

Example 3:
Input: root = []
Output: []

**Solution 1**

Empty → return
1. `res` collects results; `queue` holds nodes
2. `flag` controls direction
3. Loop while queue has items; a new `list` records each level's values
4. Inner loop: shift from queue, push to list, enqueue children
5. Flip flag; push `list` into `res`

```js binary-tree-preorder
var zigzagLevelOrder = function(root) {
    const res = [], queue = [root]
    let flag = true;
    if(!root) return res;
    while(queue.length > 0) {
        const size = queue.length;
        const list = [];
        const key = flag ? 'push' : 'unshift'
        for(let i = 0; i < size; i++){
            const r = queue.shift();
            list[key](r.val);
            if(r.left) queue.push(r.left);
            if(r.right) queue.push(r.right);
        }
        flag = !flag;
        res.push(list)
    }
    return res;
};
```

### Binary tree preorder traversal

```js
function preorderTraversal(root) {
  const result = [];
  function traverse(node) {
    if (!node) return;
    result.push(node.value);
    traverse(node.left);
    traverse(node.right);
  }
  traverse(root);
  return result;
}
```

