### How to make `a==1 && a==2 && a==3` true
```js

// Method 1: implicit coercion
(() => {
  let a = {
    i: 1,
    toString() {
      return this.i++
    }
  }

  console.log(a == 1 && a == 2 && a == 3)
})()

  // Method 2: hijack via Object.defineProperty
  ; (() => {
    let _a = 1
    Object.defineProperty(window, 'a', {
      get() {
        return _a++
      }
    })

    console.log(a == 1 && a == 2 && a == 3)
  })()

  // Method 3: hijack via Proxy
  ; (() => {
    let a = new Proxy({ i: 1 }, {
      get(target) {
        return () => target.i++
      }
    })

    console.log(a == 1 && a == 2 && a == 3)
  })()
  // Method 4: array + join
  ; (() => {
    let a = [1, 2, 3]

    a.join = a.shift

    console.log(a == 1 && a == 2 && a == 3)
  })()
  // Method 5: hidden characters
  ; (() => {
    let aﾠ = 1
    let a = 2
    let ﾠa = 3

    console.log(aﾠ == 1 && a == 2 && ﾠa == 3)
  })()

  // Method 6: with statement
  ; (() => {
    let i = 1

    with ({
      get a() {
        return i++
      }
    }) {
      console.log(a == 1 && a == 2 && a == 3)
    }
  })()

  // Method 7: sleight-of-hand — redefine `if`
  ; (() => {
    const if‌  = () => !0
    let a = 9

    if‌(a == 1 && a == 2 && a == 3)
    {
      console.log('hello world')
    }
  })()

  // Method 8: valueOf with a regex pattern match

  ; (() => {
    let a = {
      r: /\d/g,
      valueOf: function () {
        return this.r.exec(123)[0]
      }
    }

    if (a == 1 && a == 2 && a == 3) {
      console.log("hello world")
    }
  })()




```
